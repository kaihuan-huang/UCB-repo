{{#each users}}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{{get_emoji}}} {{users.name}}</h2>
  </div>
  <div class="col-md-6">
    <h3><a href="mailto:{{users.email}}">{{users.email}}</a></h3>
  </div>
</div>
{{/each}}
